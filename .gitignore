*.sw[a-z]
*/.virtualenvs
*/.config/nvim/plugged
*/.config/nvim/files

# Local secrets (AI API keys), never committed
.zshrc.env

# Claude Code config lives in its own repo (jvrsantacruz/skills), checked out
# at ~/.skills and stowed into ~/.claude
*/.claude/

# Codex config may hold gateway tokens
mac/.codex/
.codex/
