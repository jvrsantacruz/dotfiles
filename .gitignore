*.sw[a-z]
work/.config/nvim/plugged
work/.config/cases/config
*/.virtualenvs
home/.config/nvim/plugged
home/.config/nvim/files
*/.config/nvim/files

# Local secrets (AI API keys), never committed
.zshrc.env

# Claude Code config lives in its own repo (jvrsantacruz/skills)
mac/.claude/
